# DataSource settings: set here your own configurations for the database
# connection. In this example we have "netgloo_blog" as database name and
# "root" as username and password.
spring.jpa.database=POSTGRESQL
spring.database.driverClassName=org.postgresql.Driver
spring.datasource.platform=postgres

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# port configuration
server.port=${port:8080}

# ===============================
# CORS CONFIG
# ===============================
#endpoints.cors.allowed-headers=*
#endpoints.cors.allowed-methods=*
#endpoints.cors.allowed-origins=*
#endpoints.cors.exposed-headers=Access-Control-Allow-Headers,Origin,X-Requested-With,Content-Type,Accept,X-AUTH-TOKEN
#endpoints.cors.max-age=3600

# -----------------
# SECURITY TOKEN
# -----------------
# secret token generated by http://nux.net/secret
token.secret=e3b94a782fde705d21c789fc7caaddafae2ee5ac
token.expiration.minutes=700
token.lifetime.hours=24

# -----------------
# EMAIL CONFIG
# -----------------
smtp.host=smtp.gmail.com
smtp.port=465
smtp.protocol=smtp
smtp.username=biodiversitycollection@gmail.com
smtp.password=integritas12
mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.smtp.debug=true
support.email=biodiversitycollection@gmail.com

# -----------------
# UPLOAD CONFIGURATION
# -----------------
multipart.enabled=true
multipart.maxFileSize: 5000KB
multipart.maxRequestSize: 5000KB